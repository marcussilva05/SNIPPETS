{
	//Listar Drivers ODBC
	"Listar Drivers ODBC": {
		"prefix": "pysyslistodbc",
		"body": [
			"import pyodbc",
			"",
			"print(\"Drivers ODBC instalados:\")",
			"for driver in pyodbc.drivers():",
			"    print(f\"  - {driver}\")",
			""
		],
		"description": "Lista todos os drivers ODBC instalados no sistema"
	},

	//Filtrar Programas no Windows
	"Filtrar Programas no Windows": {
		"prefix": "pysysprogfilt",
		"body": [
			"${2:import winreg}",
			"",
			"x = \"${1:digite o nome do programa}\"",
			"",
			"print(f\"=== PROGRAMAS RELACIONADOS A {x} ===\\n\")",
			"",
			"# Local 1: App Paths (executáveis)",
			"caminho_apps = r\"SOFTWARE\\Microsoft\\Windows\\CurrentVersion\\App Paths\"",
			"",
			"try:",
			"    chave = winreg.OpenKey(winreg.HKEY_LOCAL_MACHINE, caminho_apps)",
			"    ",
			"    for i in range(100):",
			"        try:",
			"            nome_programa = winreg.EnumKey(chave, i)",
			"            # Filtro usando a variável x",
			"            if x in nome_programa.lower():",
			"                subchave = winreg.OpenKey(chave, nome_programa)",
			"                caminho_exe = winreg.QueryValueEx(subchave, \"\")[0]",
			"                print(f\"📌 {nome_programa}\")",
			"                print(f\"   → {caminho_exe}\\n\")",
			"                winreg.CloseKey(subchave)",
			"        except:",
			"            pass",
			"    winreg.CloseKey(chave)",
			"except:",
			"    print(\"Erro ao acessar App Paths\")",
			"",
			"# Local 2: Uninstall (programas instalados)",
			"print(f\"=== PROGRAMAS RELACIONADOS A {x} (SEM DUPLICATAS) ===\\n\")",
			"",
			"caminho_uninstall = r\"SOFTWARE\\Microsoft\\Windows\\CurrentVersion\\Uninstall\"",
			"programas_vistos = set()  # ← Para evitar duplicatas",
			"",
			"try:",
			"    chave = winreg.OpenKey(winreg.HKEY_LOCAL_MACHINE, caminho_uninstall)",
			"    ",
			"    for i in range(200):",
			"        try:",
			"            subkey_name = winreg.EnumKey(chave, i)",
			"            subchave = winreg.OpenKey(chave, subkey_name)",
			"            ",
			"            try:",
			"                nome = winreg.QueryValueEx(subchave, \"DisplayName\")[0]",
			"                if x in nome.lower():",
			"                    # Verifica se já viu este programa",
			"                    if nome not in programas_vistos:",
			"                        programas_vistos.add(nome)",
			"                        print(f\"📦 {nome}\")",
			"                        ",
			"                        try:",
			"                            versao = winreg.QueryValueEx(subchave, \"DisplayVersion\")[0]",
			"                            print(f\"   Versão: {versao}\")",
			"                        except:",
			"                            pass",
			"                        print()",
			"            except:",
			"                pass",
			"            winreg.CloseKey(subchave)",
			"        except:",
			"            pass",
			"    winreg.CloseKey(chave)",
			"except Exception as e:",
			"    print(f\"Erro: {e}\")",
			"",
			"# Local 3: Drivers ODBC",
			"print(f\"\\n=== DRIVERS ODBC {x} ===\\n\")",
			"caminho_odbc = r\"SOFTWARE\\ODBC\\ODBCINST.INI\"",
			"",
			"try:",
			"    chave = winreg.OpenKey(winreg.HKEY_LOCAL_MACHINE, caminho_odbc)",
			"    ",
			"    for i in range(50):",
			"        try:",
			"            nome_driver = winreg.EnumKey(chave, i)",
			"            # Filtro usando a variável x",
			"            if x in nome_driver.lower():",
			"                subchave = winreg.OpenKey(chave, nome_driver)",
			"                ",
			"                print(f\"🔌 {nome_driver}\")",
			"                try:",
			"                    driver_dll = winreg.QueryValueEx(subchave, \"Driver\")[0]",
			"                    print(f\"   DLL: {driver_dll}\")",
			"                except:",
			"                    pass",
			"                try:",
			"                    versao = winreg.QueryValueEx(subchave, \"DriverODBCVer\")[0]",
			"                    print(f\"   Versão ODBC: {versao}\")",
			"                except:",
			"                    pass",
			"                print()",
			"                ",
			"                winreg.CloseKey(subchave)",
			"        except:",
			"            pass",
			"    winreg.CloseKey(chave)",
			"except:",
			"    print(\"Erro ao acessar drivers ODBC\")",
			""
		],
		"description": "Lista todos os programas do filtro que estejam instalados ou App Paths ou drivers ODBC via Windows Registry"
	},

	//Listar Todos Programas Instalados
	"Listar Todos Programas Instalados": {
		"prefix": "pysysproglist",
		"body": [
			"import winreg",
			"",
			"print(\"=== TODOS PROGRAMAS INSTALADOS (COMPLETO) ===\\n\")",
			"",
			"def listar_programas(reg_key, caminho):",
			"    programas = []",
			"    try:",
			"        chave = winreg.OpenKey(reg_key, caminho)",
			"        for i in range(500):",
			"            try:",
			"                subkey_name = winreg.EnumKey(chave, i)",
			"                subchave = winreg.OpenKey(chave, subkey_name)",
			"                try:",
			"                    nome = winreg.QueryValueEx(subchave, \"DisplayName\")[0]",
			"                    if nome and nome not in programas:",
			"                        programas.append(nome)",
			"                except:",
			"                    pass",
			"                winreg.CloseKey(subchave)",
			"            except:",
			"                break",
			"        winreg.CloseKey(chave)",
			"    except:",
			"        pass",
			"    return programas",
			"",
			"# Lista de locais onde programas podem estar registrados",
			"locais = [",
			"    (winreg.HKEY_LOCAL_MACHINE, r\"SOFTWARE\\Microsoft\\Windows\\CurrentVersion\\Uninstall\"),",
			"    (winreg.HKEY_LOCAL_MACHINE, r\"SOFTWARE\\WOW6432Node\\Microsoft\\Windows\\CurrentVersion\\Uninstall\"),",
			"    (winreg.HKEY_CURRENT_USER, r\"SOFTWARE\\Microsoft\\Windows\\CurrentVersion\\Uninstall\")",
			"]",
			"",
			"todos_programas = []",
			"",
			"for reg_key, caminho in locais:",
			"    todos_programas.extend(listar_programas(reg_key, caminho))",
			"",
			"# Remove duplicatas e ordena",
			"todos_programas = sorted(set(todos_programas))",
			"",
			"for p in todos_programas:",
			"    print(f\"📦 {p}\")",
			"",
			"print(f\"\\n✅ Total: {len(todos_programas)} programas instalados\")",
			""
		],
		"description": "Lista TODOS os programas instalados no Windows (HKLM 64/32 e HKCU) sem duplicatas e ordenado"
	},

	//Listar Instâncias SQL Server
	"Listar Instâncias SQL Server": {
		"prefix": "pysyslistinstancias",
		"body": [
			"import winreg",
			"",
			"caminho = r\"SOFTWARE\\Microsoft\\Microsoft SQL Server\"",
			"chave = winreg.OpenKey(winreg.HKEY_LOCAL_MACHINE, caminho)",
			"instancias = winreg.QueryValueEx(chave, \"InstalledInstances\")[0]",
			"winreg.CloseKey(chave)",
			"",
			"for i in instancias:",
			"    print(i)",
			""
		],
		"description": "Lista instâncias SQL Server instaladas lendo do Windows Registry"
	},

//Varrer Bancos de Dados
	"Varrer Bancos de Dados": {
		"prefix": "pysysbdvarredura",
		"body": [
			"import pyodbc",
			"import winreg",
			"import socket",
			"",
			"print(\"=== INSTÂNCIAS SQL SERVER E BANCOS ===\\n\")",
			"",
			"# PASSO 1: Descobrir todas as instâncias instaladas",
			"caminho_reg = r\"SOFTWARE\\Microsoft\\Microsoft SQL Server\"",
			"nomes_instancias = []",
			"",
			"try:",
			"    chave = winreg.OpenKey(winreg.HKEY_LOCAL_MACHINE, caminho_reg)",
			"    nomes_instancias = winreg.QueryValueEx(chave, \"InstalledInstances\")[0]",
			"    winreg.CloseKey(chave)",
			"    print(f\"📋 Instâncias encontradas no registro: {nomes_instancias}\\n\")",
			"except:",
			"    print(\"⚠️ Não foi possível ler registro\\n\")",
			"",
			"# PASSO 2: Montar lista de strings de conexão",
			"# Pega o nome do computador automaticamente",
			"nome_computador = socket.gethostname()",
			"print(f\"💻 Nome do computador: {nome_computador}\\n\")",
			"",
			"# Lista de possíveis conexões (prioriza instâncias descobertas)",
			"instancias_conexao = [",
			"    \"localhost\",                    # sempre tenta",
			"    \".\",                           # ponto (mesmo que localhost)",
			"    f\"{nome_computador}\",          # nome do computador",
			"]",
			"",
			"# Adiciona as instâncias encontradas no registro",
			"for inst in nomes_instancias:",
			"    instancias_conexao.append(f\"localhost\\\\{inst}\")",
			"    instancias_conexao.append(f\"{nome_computador}\\\\{inst}\")",
			"",
			"# Remove duplicatas mantendo ordem",
			"vistas = set()",
			"instancias_conexao_unicas = []",
			"for inst in instancias_conexao:",
			"    if inst not in vistas:",
			"        vistas.add(inst)",
			"        instancias_conexao_unicas.append(inst)",
			"",
			"print(f\"🔍 Testando {len(instancias_conexao_unicas)} possíveis conexões...\\n\")",
			"",
			"# PASSO 3: Testar cada instância e listar bancos",
			"resultados = []",
			"",
			"for inst in instancias_conexao_unicas:",
			"    try:",
			"        conn = pyodbc.connect(",
			"            f'DRIVER={{ODBC Driver 17 for SQL Server}};'",
			"            f'SERVER={inst};'",
			"            'Trusted_Connection=yes;'",
			"            'Connection Timeout=3'",
			"        )",
			"        cursor = conn.cursor()",
			"        ",
			"        # Pega nome real da instância",
			"        cursor.execute(\"SELECT @@SERVERNAME\")",
			"        nome_real = cursor.fetchone()[0]",
			"        ",
			"        # Lista bancos do usuário",
			"        cursor.execute(\"\"\"",
			"            SELECT name, database_id, create_date",
			"            FROM sys.databases ",
			"            WHERE database_id > 4",
			"            ORDER BY name",
			"        \"\"\")",
			"        ",
			"        bancos = cursor.fetchall()",
			"        ",
			"        # Armazena resultado",
			"        resultados.append({",
			"            'conexao': inst,",
			"            'nome_real': nome_real,",
			"            'bancos': bancos",
			"        })",
			"        ",
			"        cursor.close()",
			"        conn.close()",
			"        ",
			"    except Exception as e:",
			"        # Ignora falhas de conexão silenciosamente",
			"        pass",
			"",
			"# PASSO 4: Exibir resultados",
			"if not resultados:",
			"    print(\"❌ Nenhuma instância SQL Server encontrada ou acessível\")",
			"else:",
			"    for i, r in enumerate(resultados):",
			"        print(f\"\\n{'='*60}\")",
			"        print(f\"🖥️  INSTÂNCIA: {r['nome_real']}\")",
			"        print(f\"   (conexão usada: {r['conexao']})\")",
			"        print('='*60)",
			"        ",
			"        if r['bancos']:",
			"            print(f\"📀 BANCOS DO USUÁRIO ({len(r['bancos'])}):\")",
			"            for b in r['bancos']:",
			"                print(f\"   • {b.name} (ID: {b.database_id})\")",
			"        else:",
			"            print(f\"📀 Nenhum banco de usuário encontrado\")",
			"            print(f\"   (apenas bancos do sistema)\")",
			"",
			"print(f\"\\n{'='*60}\")",
			"print(f\"✅ Total: {len(resultados)} instância(s) SQL Server ativa(s)\")",
			""
		],
		"description": "Varredura por Detecção automaticamente de instâncias SQL Server via registro e lista bancos de usuário"
	},

	//Listar Bancos por Instância
	"Listar Bancos por Instância": {
		"prefix": "pysysbdlist",
		"body": [
			"import pyodbc",
			"",
			"instancia = '${1:MSSQLSERVER01}'  # mude a instância",
			"",
			"try:",
			"    conn = pyodbc.connect(",
			"        f'DRIVER={{ODBC Driver 17 for SQL Server}};'",
			"        f'SERVER=localhost\\\\{instancia};'",
			"        'Trusted_Connection=yes;'",
			"    )",
			"    cursor = conn.cursor()",
			"    ",
			"    cursor.execute(\"SELECT name FROM sys.databases WHERE database_id > 4 ORDER BY name\")",
			"    ",
			"    for b in cursor.fetchall():",
			"        print(b.name)",
			"    ",
			"    cursor.close()",
			"    conn.close()",
			"except Exception as e:",
			"    print(f\"Erro: {e}\")",
			""
		],
		"description": "Lista bancos de usuário de uma instância SQL Server específica"
	},

	// CRIAR TABELA .CSV
	"CRIAR TABELA .CSV": {
		"prefix": "pytabcsv",
		"body": [
			"import pyodbc",
			"import csv",
			"",
			"instancia = '${1:NOME_DA_INSTÂNCIA}'",
			"banco = '${2:NOME_DO_BANCO}'",
			"caminho_csv = r'${3:C:\\Users\\CAMINHO\\ARQUIVO.csv}'",
			"nome_tabela = '${4:TABELA}' # NOME DA TABELA A SER CRIADA",
			"",
			"try:",
			"    conn = pyodbc.connect(",
			"        f'DRIVER={{ODBC Driver 17 for SQL Server}};'",
			"        f'SERVER=localhost\\\\{instancia};'",
			"        'Trusted_Connection=yes;'",
			"    )",
			"    cursor = conn.cursor()",
			"    ",
			"    cursor.execute(f\"USE {banco}\")",
			"    ",
			"    # Lê primeira linha com encoding 'utf-8-sig' (remove BOM automaticamente)",
			"    with open(caminho_csv, 'r', encoding='utf-8-sig') as f:",
			"        leitor = csv.reader(f, delimiter=';')  # Seu CSV usa ponto e vírgula?",
			"        colunas_originais = next(leitor)",
			"    ",
			"    # Encurta nomes das colunas (máximo 50 caracteres)",
			"    colunas = []",
			"    for col in colunas_originais:",
			"        # Remove espaços extras e encurta",
			"        col_limpa = col.strip().replace(' ', '_')[:50]",
			"        colunas.append(col_limpa)",
			"    ",
			"    # Cria a tabela com nomes encurtados",
			"    colunas_sql = ', '.join([f\"[{col}] NVARCHAR(MAX)\" for col in colunas])",
			"    cursor.execute(f\"DROP TABLE IF EXISTS {nome_tabela}\")",
			"    cursor.execute(f\"CREATE TABLE {nome_tabela} ({colunas_sql})\")",
			"    print(f\"✅ Tabela {nome_tabela} criada com {len(colunas)} colunas\")",
			"    ",
			"    # Insere os dados",
			"    with open(caminho_csv, 'r', encoding='utf-8-sig') as f:",
			"        leitor = csv.reader(f, delimiter=';')",
			"        next(leitor)",
			"        ",
			"        for linha in leitor:",
			"            # Garante que a linha tem o mesmo número de colunas",
			"            while len(linha) < len(colunas):",
			"                linha.append('')",
			"            placeholders = ', '.join(['?' for _ in range(len(colunas))])",
			"            cursor.execute(f\"INSERT INTO {nome_tabela} VALUES ({placeholders})\", linha[:len(colunas)])",
			"    ",
			"    conn.commit()",
			"    ",
			"    cursor.execute(f\"SELECT COUNT(*) FROM {nome_tabela}\")",
			"    total = cursor.fetchone()[0]",
			"    print(f\"✅ {total} linhas inseridas\")",
			"    ",
			"    cursor.execute(f\"SELECT TOP 1 * FROM {nome_tabela}\")",
			"    print(\"\\n📀 Primeira linhas:\")",
			"    for row in cursor.fetchall():",
			"        print(f\"   {row}\")",
			"    ",
			"    cursor.close()",
			"    conn.close()",
			"    ",
			"except Exception as e:",
			"    print(f\"Erro: {e}\")",
			""
		],
		"description": "Criar tabela no SQL importando dados de um arquivo .csv"
	},

	//Procurar Termo em BD/Tabelas/Colunas (NÃO DADOS) LEVE
	"Procurar Termo em BD/Tabelas/Colunas (NÃO DADOS) LEVE": {
		"prefix": "pybdfiltroleve",
		"body": [
			"#PROCURA PELO TERMO DETERMINADO EM TABELAS/COLUNAS",
			"import pyodbc",
			"",
			"termo = '${1:NOME114}'",
			"instancia = '${2:MSSQLSERVER01}'",
			"",
			"print(f\"🔍 Varredura completa por '{termo}' em {instancia}\\n\")",
			"",
			"try:",
			"    conn = pyodbc.connect(",
			"        f'DRIVER={{ODBC Driver 17 for SQL Server}};'",
			"        f'SERVER=localhost\\\\{instancia};'",
			"        'Trusted_Connection=yes;'",
			"    )",
			"    cursor = conn.cursor()",
			"    ",
			"    # Script SQL que varre tudo",
			"    sql = f\"\"\"",
			"    DECLARE @SearchTerm NVARCHAR(100) = '{termo}';",
			"    DECLARE @Results TABLE (FoundIn NVARCHAR(500));",
			"    ",
			"    -- Buscar em nomes de bancos",
			"    INSERT INTO @Results",
			"    SELECT 'database: ' + name",
			"    FROM sys.databases",
			"    WHERE name LIKE '%' + @SearchTerm + '%' AND database_id > 4;",
			"    ",
			"    -- Para cada banco, buscar em tabelas e colunas",
			"    DECLARE @db NVARCHAR(128);",
			"    DECLARE db_cursor CURSOR FOR",
			"    SELECT name FROM sys.databases WHERE database_id > 4;",
			"    ",
			"    OPEN db_cursor;",
			"    FETCH NEXT FROM db_cursor INTO @db;",
			"    ",
			"    WHILE @@FETCH_STATUS = 0",
			"    BEGIN",
			"        DECLARE @sql NVARCHAR(MAX) = '",
			"            USE [' + @db + '];",
			"            ",
			"            INSERT INTO @Results",
			"            SELECT ''' + @db + '.table: '' + TABLE_SCHEMA + ''.'' + TABLE_NAME",
			"            FROM INFORMATION_SCHEMA.TABLES",
			"            WHERE TABLE_NAME LIKE ''%'' + @SearchTerm + ''%'';",
			"            ",
			"            INSERT INTO @Results",
			"            SELECT ''' + @db + '.column: '' + TABLE_SCHEMA + ''.'' + TABLE_NAME + ''.'' + COLUMN_NAME",
			"            FROM INFORMATION_SCHEMA.COLUMNS",
			"            WHERE COLUMN_NAME LIKE ''%'' + @SearchTerm + ''%'';",
			"        ';",
			"        EXEC sp_executesql @sql, N'@SearchTerm NVARCHAR(100)', @SearchTerm;",
			"        ",
			"        FETCH NEXT FROM db_cursor INTO @db;",
			"    END",
			"    ",
			"    CLOSE db_cursor;",
			"    DEALLOCATE db_cursor;",
			"    ",
			"    SELECT * FROM @Results;",
			"    \"\"\"",
			"    ",
			"    cursor.execute(sql)",
			"    resultados = cursor.fetchall()",
			"    ",
			"    if resultados:",
			"        print(\"✅ ENCONTRADO EM:\\n\")",
			"        for r in resultados:",
			"            print(f\"   📌 {r[0]}\")",
			"    else:",
			"        print(f\"❌ Nada encontrado para '{termo}'\")",
			"    ",
			"    cursor.close()",
			"    conn.close()",
			"    ",
			"except Exception as e:",
			"    print(f\"Erro: {e}\")",
			""
		],
		"description": "Procurar Termo em Tabelas/Colunas LEVE"
	},

	//Procurar TERMO em Todos os dados das Tabelas (NÃO BD/TABELAS) PESADO
	"Procurar TERMO em Todos os dados das Tabelas (NÃO BD/TABELAS) PESADO": {
		"prefix": "pybdfiltropesado",
		"body": [
			"#PROCURA PELO TERMO DETERMINADO EM TODOS OS DADOS DA INSTÂNCIA",
			"import pyodbc",
			"",
			"termo = '${1:DIGITE_DADO}' # O que você quer encontrar",
			"instancias = ['${2:DIGITE_INSTÂNCIA}'] # instâncias que quer varrer",
			"",
			"def buscar_nos_dados(instancia, termo):",
			"    print(f\"\\n🔍 Buscando dado '{termo}' em {instancia}...\\n\")",
			"    ",
			"    try:",
			"        conn = pyodbc.connect(",
			"            f'DRIVER={{ODBC Driver 17 for SQL Server}};'",
			"            f'SERVER=localhost\\\\{instancia};'",
			"            'Trusted_Connection=yes;'",
			"        )",
			"        cursor = conn.cursor()",
			"        ",
			"        cursor.execute(\"SELECT name FROM sys.databases WHERE database_id > 4\")",
			"        bancos = [b.name for b in cursor.fetchall()]",
			"        ",
			"        encontrado = False",
			"        ",
			"        for banco in bancos:",
			"            cursor.execute(f\"USE [{banco}]\")",
			"            ",
			"            # Pega tabelas e colunas que são texto (varchar, nvarchar)",
			"            cursor.execute(\"\"\"",
			"                SELECT ",
			"                    t.name AS tabela,",
			"                    c.name AS coluna",
			"                FROM sys.tables t",
			"                JOIN sys.columns c ON t.object_id = c.object_id",
			"                WHERE c.system_type_id IN (167, 231, 239)  -- varchar, nvarchar, varchar(max)",
			"            \"\"\")",
			"            ",
			"            tabelas_colunas = cursor.fetchall()",
			"            ",
			"            for tc in tabelas_colunas:",
			"                try:",
			"                    query = f\"\"\"",
			"                        SELECT TOP 1 * ",
			"                        FROM [{banco}].[dbo].[{tc.tabela}]",
			"                        WHERE [{tc.coluna}] LIKE ?",
			"                    \"\"\"",
			"                    cursor.execute(query, (f'%{termo}%',))",
			"                    resultado = cursor.fetchone()",
			"                    ",
			"                    if resultado:",
			"                        print(f\"   🎯 ENCONTRADO: {banco}.dbo.{tc.tabela}.{tc.coluna}\")",
			"                        encontrado = True",
			"                except:",
			"                    pass",
			"        ",
			"        if not encontrado:",
			"            print(f\"   ❌ Nada encontrado para '{termo}'\")",
			"        ",
			"        cursor.close()",
			"        conn.close()",
			"        ",
			"    except Exception as e:",
			"        print(f\"   ⚠️ Erro: {e}\")",
			"",
			"for inst in instancias:",
			"    buscar_nos_dados(inst, termo)",
			""
		],
		"description": "Procurar Termo nos DADOS das Tabelas (NÃO BD/TABELAS) PESADO"
},


//********************************************************************** Web Scraping (web) **********************************************************************

	// abrir_navegador_em_modo_debug
"abrir_navegador_em_modo_debug": {
	"prefix": "webabrirnav",
	"body": [
		"# porta padrão 9222, mas pode ser qualquer porta disponível entre 1024 e 65535",
		"# host padrão para qualquer máquina: 127.0.0.1 ou localhost",
		"# debug edge: Start-Process msedge -ArgumentList \"--remote-debugging-port=9222\", \"--user-data-dir=C:\\temp\\edge_debug\"",
		"# debug chrome: Start-Process chrome -ArgumentList \"--remote-debugging-port=9222\", \"--user-data-dir=C:\\temp\\chrome_debug\"",
		"# debug chrome: Start-Process firefox -ArgumentList \"-start-debugger-server", "9222\", \"-no-remote", "-profile", "C:\\temp\\firefox_debug_profile\""
	],
	"description": "Abre o Site via CMD ADM para usar com Selenium via Debug"
},

// cabeçalho web scraping
    "cabecalho_web_scraping": {
        "prefix": "webcab",
        "body": [
        "# Extrator para: CADASTRO = EVENTO → extrair motivos da 3ª coluna",
        "# 1) z py_venv_ativar",
        "# 2) z sys_edge_modo_debug ou ( Start-Process msedge -ArgumentList \"--remote-debugging-port=9222\", \"--user-data-dir=C:\\temp\\edge_debug\" ) ou Google Chrome ( Start-Process chrome -ArgumentList \"--remote-debugging-port=9222\", \"--user-data-dir=C:\\temp\\chrome_debug\" )",
        "# 3) Site https://sed.educacao.sp.gov.br/Funcional/Consulta/Index",
        "# 4) python teste.py",
        "",
        "from selenium import webdriver",
        "from selenium.webdriver.edge.options import Options",
        "from selenium.webdriver.common.by import By",
        "from selenium.webdriver.support.ui import Select, WebDriverWait",
        "from selenium.webdriver.support import expected_conditions as EC",
        "import time",
        "import csv",
        "import os",
        "from datetime import datetime"
    ],
    "description": "Cabecalho extrator para CADASTRO = EVENTO (motivos 3a coluna)"
},

	// requirements webscraping
"webrequisitos": {
    "prefix": "webrequirements",
    "body": [
        "attrs==26.1.0",
        "certifi==2026.4.22",
        "cffi==2.0.0",
        "charset-normalizer==3.4.7",
        "h11==0.16.0",
        "idna==3.13",
        "outcome==1.3.0.post0",
        "packaging==26.2",
        "pycparser==3.0",
        "PySocks==1.7.1",
        "python-dotenv==1.2.2",
        "requests==2.33.1",
        "selenium==4.43.0",
        "sniffio==1.3.1",
        "sortedcontainers==2.4.0",
        "trio==0.33.0",
        "trio-websocket==0.12.2",
        "typing_extensions==4.15.0",
        "urllib3==2.6.3",
        "webdriver-manager==4.0.2",
        "websocket-client==1.9.0",
        "wsproto==1.3.2"
    ],
    "description": "Lista de dependencias para web scraping com Selenium"
},

"leitor_arquivo_do_pc": {
    "prefix": "weblerarquivopc",
    "body": [
        "caminho = r\"${1:C:\\\\caminho\\\\para\\\\arquivo.txt}\"",
        "def ${2:ler_linhas}(caminho,",
        "               ignorar_vazias=${3:True},        # ignorar linhas que ficaram vazias após strip",
        "               remover_acentos=${4:False},      # normalizar acentos (ex: 'coração' -> 'coracao')",
        "               converter_maiusculas=${5:False}, # transformar tudo em maiúsculas",
        "               converter_minusculas=${6:False}, # transformar tudo em minúsculas",
        "               remover_pontuacao=${7:False},    # remove tudo que não for dígito (CPF/CNPJ)",
        "               manter_apenas_digitos=${8:False},# igual a remover_pontuacao (sinônimo)",
        "               filtrar_por_prefixo=${9:None},   # ex: \"CPF:\" -> só linhas começando com isso",
        "               filtrar_por_sufixo=${10:None},    # ex: \"@sp.gov.br\" -> só linhas terminando assim",
        "               pular_linhas_iniciais=${11:0},    # pular N primeiras linhas",
        "               limitar_linhas=${12:None},        # ler no máximo N linhas (int)",
        "               remover_duplicatas=${13:False},   # mantém apenas a primeira ocorrência",
        "               comentario_inicio='${14:#}',      # ignorar linhas que começam com este caractere",
        "               strip_caracteres=${15:None},      # strip específico (ex: ' \\t\\n\\r')",
        "               validar_cpf=${16:False},          # só adiciona se for um CPF válido",
        "               validar_email=${17:False}):       # só adiciona se tiver formato de e-mail",
        "    ",
        "    import re",
        "    linhas = []",
        "    vistos = set()",
        "    ",
        "    with open(caminho, 'r', encoding='utf-8') as f:",
        "        for idx, linha in enumerate(f):",
        "            # 1. Extrair valor (strip)",
        "            if strip_caracteres is None:",
        "                valor = linha.strip()",
        "            else:",
        "                valor = linha.strip(strip_caracteres)",
        "            ",
        "            # 2. Pular linhas iniciais (antes de qualquer filtro)",
        "            if idx < pular_linhas_iniciais:",
        "                continue",
        "            ",
        "            # 3. Ignorar vazias",
        "            if ignorar_vazias and not valor:",
        "                continue",
        "            ",
        "            # 4. Ignorar comentários",
        "            if comentario_inicio and valor.startswith(comentario_inicio):",
        "                continue",
        "            ",
        "            # 5. Remover acentos",
        "            if remover_acentos:",
        "                import unicodedata",
        "                valor = unicodedata.normalize('NFKD', valor).encode('ascii', 'ignore').decode('ascii')",
        "            ",
        "            # 6. Maiúsculas/minúsculas",
        "            if converter_maiusculas:",
        "                valor = valor.upper()",
        "            elif converter_minusculas:",
        "                valor = valor.lower()",
        "            ",
        "            # 7. Remover pontuação (dígitos apenas)",
        "            if remover_pontuacao or manter_apenas_digitos:",
        "                valor = re.sub(r'\\D', '', valor)",
        "            ",
        "            # 8. Filtrar por prefixo",
        "            if filtrar_por_prefixo:",
        "                if not valor.startswith(filtrar_por_prefixo):",
        "                    continue",
        "                # opcional: remover o prefixo (descomente a linha abaixo)",
        "                # valor = valor[len(filtrar_por_prefixo):].strip()",
        "            ",
        "            # 9. Filtrar por sufixo",
        "            if filtrar_por_sufixo:",
        "                if not valor.endswith(filtrar_por_sufixo):",
        "                    continue",
        "            ",
        "            # 10. Validar CPF",
        "            if validar_cpf:",
        "                cpf_limpo = re.sub(r'\\D', '', valor)",
        "                if len(cpf_limpo) != 11 or not cpf_limpo.isdigit():",
        "                    continue",
        "                def _validar_cpf(c):",
        "                    if len(set(c)) == 1: return False",
        "                    soma = sum(int(c[i]) * (10-i) for i in range(9))",
        "                    d1 = 11 - (soma % 11)",
        "                    d1 = 0 if d1 >= 10 else d1",
        "                    soma = sum(int(c[i]) * (11-i) for i in range(10))",
        "                    d2 = 11 - (soma % 11)",
        "                    d2 = 0 if d2 >= 10 else d2",
        "                    return d1 == int(c[9]) and d2 == int(c[10])",
        "                if not _validar_cpf(cpf_limpo):",
        "                    continue",
        "                valor = cpf_limpo",
        "            ",
        "            # 11. Validar e-mail",
        "            if validar_email:",
        "                if not re.match(r'^[\\w\\.-]+@[\\w\\.-]+\\.\\w+$', valor):",
        "                    continue",
        "            ",
        "            # 12. Remover duplicatas (após todas as transformações)",
        "            if remover_duplicatas:",
        "                valor_chave = valor.lower() if converter_minusculas else valor",
        "                if valor_chave in vistos:",
        "                    continue",
        "                vistos.add(valor_chave)",
        "            ",
        "            # 13. Adicionar à lista",
        "            linhas.append(valor)",
        "            ",
        "            # 14. Limitar número de linhas",
        "            if limitar_linhas and len(linhas) >= limitar_linhas:",
        "                break",
        "    ",
        "    return linhas",
        "",
        "# ===== Exemplo de uso =====",
        "dados = ${2:ler_linhas}(caminho,",
        "                        ignorar_vazias=${3:True},",
        "                        remover_acentos=${4:False},",
        "                        converter_maiusculas=${5:False},",
        "                        converter_minusculas=${6:False},",
        "                        remover_pontuacao=${7:False},",
        "                        manter_apenas_digitos=${8:False},",
        "                        filtrar_por_prefixo=${9:None},",
        "                        filtrar_por_sufixo=${10:None},",
        "                        pular_linhas_iniciais=${11:0},",
        "                        limitar_linhas=${12:None},",
        "                        remover_duplicatas=${13:False},",
        "                        comentario_inicio='${14:#}',",
        "                        strip_caracteres=${15:None},",
        "                        validar_cpf=${16:False},",
        "                        validar_email=${17:False})",
        "print(f\"Foram lidos {len(dados)} registro(s).\")",
        "for i, item in enumerate(dados[:5], 1):",
        "    print(f\"{i}: {item}\")"
    ],
    "description": "Função completa para leitura de arquivos com inúmeras opções (ordem corrigida)"
},
// cola_de_cores
"cola_de_cores": {
    "prefix": "webcolacores",
    "body": [
        "# Cores de texto (foreground) - Normal",
        "print(f\"\\n\\n \\033[30m TEXTO PRETO \\033[0m \\n\\n\")",
        "print(f\"\\n\\n \\033[31m TEXTO VERMELHO \\033[0m \\n\\n\")",
        "print(f\"\\n\\n \\033[32m TEXTO VERDE \\033[0m \\n\\n\")",
        "print(f\"\\n\\n \\033[33m TEXTO AMARELO \\033[0m \\n\\n\")",
        "print(f\"\\n\\n \\033[34m TEXTO AZUL \\033[0m \\n\\n\")",
        "print(f\"\\n\\n \\033[35m TEXTO MAGENTA \\033[0m \\n\\n\")",
        "print(f\"\\n\\n \\033[36m TEXTO CIANO \\033[0m \\n\\n\")",
        "print(f\"\\n\\n \\033[37m TEXTO BRANCO \\033[0m \\n\\n\")",
        "",
        "# Cores de texto (foreground) - Brilhante",
        "print(f\"\\n\\n \\033[90m TEXTO PRETO BRILHANTE \\033[0m \\n\\n\")",
        "print(f\"\\n\\n \\033[91m TEXTO VERMELHO BRILHANTE \\033[0m \\n\\n\")",
        "print(f\"\\n\\n \\033[92m TEXTO VERDE BRILHANTE \\033[0m \\n\\n\")",
        "print(f\"\\n\\n \\033[93m TEXTO AMARELO BRILHANTE \\033[0m \\n\\n\")",
        "print(f\"\\n\\n \\033[94m TEXTO AZUL BRILHANTE \\033[0m \\n\\n\")",
        "print(f\"\\n\\n \\033[95m TEXTO MAGENTA BRILHANTE \\033[0m \\n\\n\")",
        "print(f\"\\n\\n \\033[96m TEXTO CIANO BRILHANTE \\033[0m \\n\\n\")",
        "print(f\"\\n\\n \\033[97m TEXTO BRANCO BRILHANTE \\033[0m \\n\\n\")",
        "",
        "# Cores de fundo (background) - Normal",
        "print(f\"\\n\\n \\033[40m FUNDO PRETO \\033[0m \\n\\n\")",
        "print(f\"\\n\\n \\033[41m FUNDO VERMELHO \\033[0m \\n\\n\")",
        "print(f\"\\n\\n \\033[42m FUNDO VERDE \\033[0m \\n\\n\")",
        "print(f\"\\n\\n \\033[43m FUNDO AMARELO \\033[0m \\n\\n\")",
        "print(f\"\\n\\n \\033[44m FUNDO AZUL \\033[0m \\n\\n\")",
        "print(f\"\\n\\n \\033[45m FUNDO MAGENTA \\033[0m \\n\\n\")",
        "print(f\"\\n\\n \\033[46m FUNDO CIANO \\033[0m \\n\\n\")",
        "print(f\"\\n\\n \\033[47m FUNDO BRANCO \\033[0m \\n\\n\")",
        "",
        "# Cores de fundo (background) - Brilhante",
        "print(f\"\\n\\n \\033[100m FUNDO PRETO BRILHANTE \\033[0m \\n\\n\")",
        "print(f\"\\n\\n \\033[101m FUNDO VERMELHO BRILHANTE \\033[0m \\n\\n\")",
        "print(f\"\\n\\n \\033[102m FUNDO VERDE BRILHANTE \\033[0m \\n\\n\")",
        "print(f\"\\n\\n \\033[103m FUNDO AMARELO BRILHANTE \\033[0m \\n\\n\")",
        "print(f\"\\n\\n \\033[104m FUNDO AZUL BRILHANTE \\033[0m \\n\\n\")",
        "print(f\"\\n\\n \\033[105m FUNDO MAGENTA BRILHANTE \\033[0m \\n\\n\")",
        "print(f\"\\n\\n \\033[106m FUNDO CIANO BRILHANTE \\033[0m \\n\\n\")",
        "print(f\"\\n\\n \\033[107m FUNDO BRANCO BRILHANTE \\033[0m \\n\\n\")",
        "",
        "# Estilos e reset",
        "print(f\"\\n\\n \\033[1m NEGRITO \\033[0m \\n\\n\")",
        "print(f\"\\n\\n \\033[4m SUBLINHADO \\033[0m \\n\\n\")",
        "print(f\"\\n\\n \\033[7m FUNDO E TEXTO INVERTIDOS \\033[0m \\n\\n\")",
        "print(f\"\\n\\n \\033[0m RESET TOTAL \\033[0m \\n\\n\")"
    ],
    "description": "Exibe todas as cores ANSI (texto e fundo) no terminal"
}

}